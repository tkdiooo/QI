<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>backstage-dictionary</artifactId>
        <groupId>com.qi.backstage.dictionary</groupId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>dictionary-server</artifactId>
    <packaging>jar</packaging>

    <dependencies>
        <!-- spring-cloud-config-client -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-config-client</artifactId>
        </dependency>
        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.sfsctech.common</groupId>
            <artifactId>common-cloud-client</artifactId>
            <version>${sfsctech.common.version}</version>
        </dependency>
        <dependency>
            <groupId>com.sfsctech.framework</groupId>
            <artifactId>sfsctech-mybatis-starter</artifactId>
            <version>1.0.0</version>
        </dependency>
        <!-- support-bootstrap support -->
        <dependency>
            <groupId>com.qi.support</groupId>
            <artifactId>support-bootstrap</artifactId>
            <version>1.0.0-SNAPSHOT</version>
        </dependency>
        <!-- dictionary-interface support -->
        <dependency>
            <groupId>com.qi.backstage.dictionary</groupId>
            <artifactId>dictionary-interface</artifactId>
            <version>${dictionary.version}</version>
        </dependency>
        <!-- dictionary-dao support -->
        <dependency>
            <groupId>com.qi.backstage.dictionary</groupId>
            <artifactId>dictionary-dao</artifactId>
            <version>${dictionary.version}</version>
        </dependency>
    </dependencies>

    <build>
        <finalName>Dictionary-Server</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.qi.dictionary.server.Runner</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>
                                repackage
                            </goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>


</project>